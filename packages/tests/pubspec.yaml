# ignore_for_file sort_pub_dependencies
name: tests
description: An app for FlutterFire testing (e2e).
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  # firebase_core: 2.4.1
  # firebase_core_tizen: ^1.0.0
  # firebase_database: 10.0.9
  # firebase_database_tizen: ^0.1.0
  # cloud_functions: 4.0.7
  # cloud_functions_tizen: ^0.1.0
  flutter:
    sdk: flutter

dependency_overrides:
  # firebase_core_tizen:
  #   path: ../firebase_core
  # firebase_database_tizen:
  #   path: ../firebase_database
  # cloud_functions_tizen:
  #   path: ../cloud_functions

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  integration_test_tizen: ^2.0.1

flutter:
  uses-material-design: true
